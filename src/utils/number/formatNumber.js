/**
 * Formats number to locale string
 * @param  {any} n - value.
 * @return {boolean} - Comma separated value.
 */

const formatNumber = n => Number(n).toLocaleString();

export default formatNumber;
